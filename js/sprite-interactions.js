
function doSpriteInteraction() {
    //     // SPRITE INTERACTIONS
    //     // reset the coin when the player touches it
    //     if (player.isTouching(coin)) {
    //         coin.x = randomNumber(20, 380);
    //         coin.y = randomNumber(20, 380);
    //         score = score + 1;
    //     }
    //     // make the obstacles disappear and decrease score if player touches it
    //     if (player.isTouching(card1)) {
    //         card1.visibility = false;
    //         score = score - 1;
    //     }
    //     if (player.isTouching(card2)) {
    //         card2.visibility = false;
    //         score = score - 1;
    //     }
    //     if (player.isTouching(card3)) {
    //         card3.visibility = false;
    //         score = score - 1;
    //     }
    //     if (player.isTouching(card4)) {
    //         card4.visibility = false;
    //         score = score - 1;
    //     }
    //     // make the obstacles push the player
    //     if (player.isTouching(platform1)) {
    //         platform1.displace("player");
    //     }
    //     if (player.isTouching(platform2)) {
    //         platform2.displace("player");
    //     }
}